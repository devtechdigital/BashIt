# For CI/deploy-only: copy to .env.local and set CONVEX_DEPLOY_KEY.
# Get the production deploy key from:
# https://dashboard.convex.dev → your *production* deployment → Settings → "URL and Deploy Key"
# Prod deployment (where convex deploy pushes): brainy-setter-166
CONVEX_DEPLOYMENT=abundant-perch-26
CONVEX_DEPLOY_KEY=prod:brainy-setter-166|your-key-here
